# js-basics

## Git commands

```
git clone <url> - cloning a repo in local
git checkout -b <branch-name> - create new branch and checkout to that
git checkout <branch-name> - checkout to branch
git commit -m "message" - commit code locally
git push - push code to server
git pull - pull changes from server
```
